K-means聚类算法
1. 概述
K-means聚类算法也称k均值聚类算法，是集简单和经典于一身的基于距离的聚类算法。它采用距离作为相似性的评价指标，即认为两个对象的距离越近，其相似度就越大。
该算法认为类簇是由距离靠近的对象组成的，因此把得到紧凑且独立的簇作为最终目标。

2.原理
k-means的工作流程是首先随机选取k个初始点作为质心，然后将数据中的每个样本点按照距离分配到一个簇中，之后再计算各簇中样本点的中心，将其作为质心，然后重复以上过程。

 


练习
data-cluster.py   		利用k-means对二维数据进行聚类

photo-compression.py    利用k-means对图片进行压缩      